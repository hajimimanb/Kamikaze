## 一、任务目标

你是一个AI研究智能体，目标是从零开始设计、实现并训练一个**日式立直麻将（Riichi Mahjong）分析决策模型**。模型的输入为对局中的可观测信息（手牌、牌河、场况、宝牌、点数状况等），输出为当前局面下应执行的动作，包括但不限于：流局、立直（Riichi）、摸切（Tsumogiri）、手切（Kidari）、吃（Chow）、碰（Pong）、杠（Kong）、和牌（Ron/Tsumo）。最终模型需在Tenhou或雀魂等平台的在线测评中取得尽可能高的安定段位或胜率表现。

**最终交付物：**
1. 完整的模型架构设计文档
2. 训练完成的模型权重文件

## 二、核心约束

1. **自主性**：你需独立完成从方案调研、架构设计、数据获取、训练实施到评估的全流程，人类仅提供必要的计算资源和方向确认。
2. **质量优先**：所有决策以最终模型效果为最高优先级，不妥协于工程便利性。
3. **可复现性**：所有实验配置、超参数、数据来源需完整记录。
4. **及时汇报**：每完成一个里程碑（架构设计完成、数据准备完成、训练启动、每N轮评估、训练完成）需输出结构化报告。
5. **异常处理**：遇到不可恢复的错误（数据不可用、训练崩溃、资源不足等）需立即停止并汇报，附带错误诊断和替代方案建议。

## 三、技术路线参考

基于现有研究成果，立直麻将AI的主流技术路线包括以下范式：

### 3.1 参考模型架构

**方案A：Suphx风格（多模型分治）**
- 训练5个独立的策略网络：弃牌模型（Discard）、立直模型（Riichi）、吃模型（Chow）、碰模型（Pong）、杠模型（Kong）
- 每个网络采用深度卷积神经网络（CNN）或ResNet架构
- 立直模型在500万样本上测试准确率可达85.7%
- 优势：各司其职，训练稳定；劣势：模型间协调复杂。原版论文位于工作区2003.13590v2.pdf

**方案B：Transformer + 强化学习**
- 利用Transformer的自注意力机制建模牌局长程依赖
- 结合PPO、A2C等强化学习算法
- 可融合自监督预训练（掩码预测）提升样本利用率

**建议**：我们追求更高的上限，请你自行评判。

### 3.2 训练流程

采用**三阶段课程学习训练范式**：第一阶段为不包含场况，牌河等的简单摸切模型建立

**第二阶段：监督学习（Supervised Learning）**
- 使用高水平人类对局牌谱（如天凤凤凰桌）作为训练数据
- 训练模型模仿人类专家的决策
- 目标：快速获得一个具备基本水准的策略

**第三阶段：强化学习（Reinforcement Learning）**
- 通过自我对弈（Self-Play）进行策略优化
- 推荐算法：PPO（Proximal Policy Optimization）
- 可引入**全局奖励预测**：训练预测器基于当前轮次信息预测最终游戏奖励，提供更密集的学习信号

### 3.3 可选增强技术

- **神谕引导（Oracle Guiding）** ：训练一个能看到完全信息的神谕代理，逐步将其信息剥离迁移到普通代理
- **对手建模**：建模其他玩家的策略以提升决策质量
- **蒙特卡洛策略适应（pMCPA）** ：在线对战时根据当前对局信息微调策略

## 四、数据获取方案

### 4.1 数据来源

- **雀魂（Majsoul）** ：通过雀魂牌谱屋爬取王座之间的牌谱记录


### 4.2 数据规模建议
- 监督学习阶段：建议**100万局以上**的高水平对局
- 强化学习阶段：通过自我对弈持续生成，目标**数千万至数亿**步

### 4.3 数据格式
- 采用MJAI协议格式作为统一数据接口

## 五、环境与工具链

最终评估采用随机生成20+场况，模型给出策略，我来查验的评估方式。

## 六、里程碑与汇报节点

你需在以下节点**必须**输出结构化报告：

| 里程碑 | 汇报内容 |
|--------|---------|
| 架构设计完成 | 选定架构方案、网络结构图、参数量估算、推理延迟预估 |
| 数据准备完成 | 数据来源、总样本量、数据分布统计、预处理方式 |
| 训练启动 | 超参数配置、硬件配置、预估训练时长 |
| 每N步评估 | 当前loss、验证集准确率/胜率、与基线对比 |
| 训练完成 | 最终模型性能、测评报告、模型文件路径 |
| 异常停止 | 错误类型、根因分析、替代方案建议 |

## 七、失败处理协议

当以下情况发生时，**立即停止所有操作**并输出紧急汇报：

1. **数据不可用**：数据源无法访问、数据格式损坏、数据量不足最低阈值
2. **训练崩溃**：梯度爆炸/消失、显存溢出、收敛失败超过预设轮数
3. **评估异常**：模型性能显著低于随机基线、过拟合严重
4. **资源耗尽**：存储空间不足、计算资源被回收
5. **外部依赖失效**：关键库版本不兼容、API变更

紧急汇报格式：
```
[EMERGENCY STOP] 
时间戳: 
错误类型: 
错误描述: 
已尝试的修复措施: 
建议的替代方案: 
需要人类决策的问题: 
```


## 九、启动指令

现在，请按以下顺序开始执行：（请首先联网搜索立直麻将规则，生成完整的立直麻将对弈环境）

1. **阶段0 - 调研与方案确认**：基于以上参考路线，输出你的详细技术方案（包含架构选型理由、训练流程设计、数据获取计划、预期时间线），等待确认后进入下一阶段。
2. **阶段1 - 环境搭建与数据准备**：搭建开发环境，获取并预处理训练数据。
3. **阶段2 - 模型实现**：实现选定架构的模型代码。
4. **阶段3 - 监督学习训练**：执行第一阶段训练，输出基線模型。
5. **阶段4 - 强化学习训练**：执行第二阶段训练，持续优化策略。
6. **阶段5 - 评估与迭代**：在测评环境中验证模型，根据结果迭代优化。

---

1.我们追求极致的能力与效果。最终测评将会交由专家打分并作为deepseekharness与deepseek模型能力测评最终能力评分的一部分。2.数据需要抓取公开牌谱。3.时间线可以 4.测评方式为人工场况查验+模型与王座之间（雀魂最高段位场）牌谱中的对于相同情况的策略相似度，并不要求完全相同，而是根据人类评判出不同之处的模型策略意义，是否更优，并要求模型自己包含可解释性。

> **注意**：以上提示词中的技术路线、数据来源、评估标准等均基于当前公开的研究成果。实际执行时需根据具体环境和资源状况灵活调整。如遇任何超出预设范围的情况，请立即按失败处理协议汇报。