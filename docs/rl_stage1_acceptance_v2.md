# RL 阶段 1 计划二轮修订复验报告（auditor / t3）

> 复验日期：2026-08-26 ｜ 对象：二轮修订（train_rl.py / policy.py / eval_vs_sl.py / tools/test_rl_grads.py / docs/rl_stage1_arch.md §7.7）
> 方法：逐条核验 6 项修复清单 + 独立实证（复现 t2 的 ratio=1.26 场景、实跑官方测试、实跑 smoke 与 eval v2）

## 0. 结论

**达标（通过）—— 完善度评分 82/100。非敷衍、具体可操作；修复清单 6 项全部落地且经实证验证。**
剩余问题全部为文档一致性残留与低优先级项（不阻塞进入训练准备，建议 <0.5 人·时清理）。

## 1. 修复清单逐项核验（全部实跑/实测）

| # | 声称修复 | 核验结果 | 证据 |
|---|---|---|---|
| 1 | logp 归一化（声明阶段双选项用 _claim_probs） | ✅ 已修复 | policy.py:341-346；**复现 t2 的 1.26 场景（pon logit 0.8 + chow -0.5）→ 三者 replay=sampling，diff=0.000000** |
| 2 | eval_vs_sl.py v2（100局/座、3种子、CI+下界、事件接线） | ✅ 已落地 | eval_vs_sl.py:47-48（--games 100/--seeds 3）、:70-74（mean±1.96se 与 mean≥0.66 且 lo≥0.60）、:32/34（policy(game) 真实时间线）；**实跑 --games 1 --seeds 1：输出 seed/mean/CI/判定/0.375 参考，逻辑正确** |
| 3 | smoke 增加 abs(ratio-1)<0.05 断言 | ✅ 已加 | train_rl.py:214；**实跑 --smoke --event-attn：SMOKE OK (ratio=1.0000)** |
| 4 | 文档回填（gate=-2、事件源、Bernoulli、验收 100×3） | ⚠️ 大部分完成，4 处残留 | §4.3:86 gate=-2 ✅、§2.2:36 事件源 ✅、§5/§6.1 Bernoulli ✅、§8:196-197 100×3+下界 ✅、§10.4:223 ✅；**残留见 §3** |
| 5 | 集成梯度测试 + 可解释性修正 + 熵对标 + §7.7 | ✅ 测试通过 / ⚠️ §4.3 未同步 | **实跑 tools/test_rl_grads.py：3/3 PASS（双选项 logp、ron+discard logp、critic/policy 梯度非零 + ratio=1.0000）**；§7.7:240-248 记录完整；但 §4.3:87 旧可解释性 claim 仍在（见 §3-A） |
| 6 | device 修复（_claim_probs 用 logits device） | ✅ 已修复 | policy.py:326-327（dev 取 logits device）、:279/:306（.cpu().numpy()） |

## 2. 新增实测亮点

- **1 打 3 均势期望验证**：eval v2 小样本实跑（value_pretrain vs transfer_final，同主干），4 局 rank_dist=[1,1,1,1]，win_rate=0.375 —— 与评审 §2.5 的 0.375 期望完全吻合，门槛校准逻辑正确。
- **t2 全部三项高严重度正确性缺陷现均闭环**：critic/熵梯度（§2.1）→ 梯度测试 PASS；logp 分布一致（§2.2）→ 1.26 场景修复 + 断言；事件接线（§2.3）→ 训练侧 + eval 侧均经真实时间线。

## 3. 剩余问题（按优先级，全部不阻塞）

- **A【中·文档】§4.3:87 与 §7.7:246 自相矛盾**：§4.3 仍写"训练期望: gate 逐通道分化（river 组大、hand/shanten 组小）——旁支合理性的可解释证据"，而 §7.7 声称"不再宣称 river/hand 组对应"。评审 §1.2 点名的错误 claim 实际未从正文删除（只写了修正记录）。建议删除/改写 §4.3:87。
- **B【中·文档】§2.2:47 与 §2.2:36 矛盾**：:36 已写明 RL 决策用 build_events_from_game（严格时序 20/20），:47 仍留"⚠️ 已知近似: 跨家事件按座位拼接…阶段1消融评估影响"（旧 build_events 的说明）。建议改为仅描述离线回退场景。
- **C【低·文档】§7.4 两处与 §5/§6.1/§7.7 不一致**：:172 仍硬编码 "H_target = 0.35"（§7.7:248 已改为 min(0.35, H_bar_sl+0.1)）；:171 仍写"ron/tsumo/kyushu 为阈值决策（非采样）"（§5/§6.1 已改为 RL 期 Bernoulli）。建议同步。
- **D【低·文档】陈旧引用**：§1 总览图与 §9 索引仍写 build_events（应为 build_events_from_game）；§7.3 实现列表 "…/_recompute_logp"（该函数不存在，实为 policy._logp_for_action）；§9 eval_vs_sl.py 未注 v2；net.py:138/141 注释仍写"初始 gate=-5"（代码实际 -2）。
- **E【低·方法】eval CI 用 z=1.96 而非 t 分布**：默认 3 种子 df=2 时 95% t≈4.30，z 法 CI 偏窄，判定略宽松。建议 seeds<10 时用 t 值。
- **F【低·前存】ron-only 声明态 pass 的 logp 记 0（log 1）**而非 log(1-p_ron)（policy.py:298-299）；采样与重放自洽（ratio=1 不破），但梯度权重略偏。二轮清单外的前存近似，可留待训练期观察。
- **G【低·未回应】价值尺度核对**（t2 遗留）：smoke 初值 value loss≈68.7 远大于 policy loss≈0，value 初值尺度与奖励尺度（得分差/100000）不匹配；已做 adv 归一化缓解，但未明确核对 value_pretrain 尺度。建议开训前加一行归一化/尺度说明。
- **H【低·计划-实现】§7.2-7.4 未实现项（对手池/32局池/minibatch/分层lr/熵自适应）仍按规格书写**，仅在 §11 标注"已采纳但后置"。建议各节加一行"（后置，未实现）"状态标记，避免"目标设计 vs 骨架"歧义再现。

## 4. 评分

- 逐条回应评审：≈90%（三高缺陷 + 验收 + 测试 + 可解释性/熵对标全部有实质动作）
- 落地与可验证：95%（全部实跑实证，仅 §4.3 可解释性正文未删）
- 文档一致性：70%（4 处残留矛盾，§7.7 记录优于正文）
- **综合：82/100 —— 达标**；建议按 §3-A/B/C/D 快速清理（<0.5 人·时）后即可进入训练准备。
